interface:
  display_name: "Agent Self Audit"
  short_description: "Run a concise preflight audit before substantial repo work"
  default_prompt: "Use $agent-self-audit to verify task routing, repo guidance coverage, relevant mistake rules, assessment coverage, required artifacts, path conventions, blockers, and validation expectations before implementation."
policy:
  allow_implicit_invocation: true
