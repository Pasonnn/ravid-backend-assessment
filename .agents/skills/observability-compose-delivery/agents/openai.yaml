interface:
  display_name: "Observability Compose Delivery"
  short_description: "Implement logs, dashboards, and Docker delivery"
  default_prompt: "Use $observability-compose-delivery to implement or review structured logging, Alloy and Loki config, Grafana provisioning, Docker Compose setup, healthchecks, and delivery docs for the assessment."
policy:
  allow_implicit_invocation: true
